name: Build and Release

on:
  push:
    branches:
      - master

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: dotnet restore VRChat_Screenshot_Discord/VRChat_Screenshot_Discord.vbproj

    - name: Build the project
      run: dotnet build VRChat_Screenshot_Discord/VRChat_Screenshot_Discord.vbproj --configuration Release

    - name: Publish the project
      run: dotnet publish VRChat_Screenshot_Discord/VRChat_Screenshot_Discord.vbproj --configuration Release --output ./output

    - name: Create ZIP file
      id: create_zip
      run: |
        $VERSION = (Get-Date -Format "yyyyMMdd")
        $COUNTER = 1
        $ZIP_NAME = "VSD-VRChatScreenshotDiscord_V$VERSION_{0:D3}.zip" -f $COUNTER
        while (Test-Path "./output/$ZIP_NAME") {
          $COUNTER++
          $ZIP_NAME = "VSD-VRChatScreenshotDiscord_V$VERSION_{0:D3}.zip" -f $COUNTER
        }
        Compress-Archive -Path ./output/* -DestinationPath "./output/$ZIP_NAME"
        echo "::set-output name=zip_path::./output/$ZIP_NAME"
      shell: pwsh

    - name: Create GitHub Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: v${{ github.sha }}
        release_name: Release V${{ github.sha }}
        body: |
          **Release Notes**
          - Changes in this release.
        draft: false
        prerelease: false

    - name: Upload Release Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ${{ steps.create_zip.outputs.zip_path }}
        asset_name: VSD-VRChatScreenshotDiscord.zip
        asset_content_type: application/zip
